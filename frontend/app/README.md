# frontend/app

Маршруты Next.js (App Router). Каждая папка `*/page.tsx` — отдельная страница панели;
большинство — клиентские компоненты (`"use client"`), которые ходят на бэкенд через
`lib/api-client.ts` с куки-аутентификацией.

## Каркас
- `layout.tsx` — корневой layout (провайдеры, шрифты, обёртка).
- `page.tsx` — главная страница **«Обзор»** (дашборд: сводка по хостам/задачам/отчётам).

## Страницы
- `hosts/page.tsx` — **Хосты**: реестр узлов, добавление/редактирование, проверка
  доступности, перепривязка SSH-ключа, управление группами, режим «Доска».
- `modules/page.tsx` — **Модули**: список модулей, загрузка/включение, схемы.
- `run/page.tsx` — **Запуск задачи**: выбор модуля и цели, аргументы, live-статус.
- `history/page.tsx` — **История**: прошлые запуски задач и их результаты.
- `scheduled/page.tsx` — **Планировщик**: запланированные/повторяющиеся задачи.
- `inventory/page.tsx` — **Инвентаризация**: снимки оборудования/ОС по хостам.
- `reports/page.tsx` — **Отчёты**: генерация и выгрузка отчётов (TXT/CSV/JSON).
- `keys/page.tsx` — **Ключи**: SSH-ключи (загрузка, генерация, ключ по умолчанию).
- `status/page.tsx` — статусная страница (состояние сервисов/соединения).
- `admin/page.tsx` — **Администрирование**: пользователи, доступы, бэкап/восстановление БД.
- `login/page.tsx` — форма входа.

## Серверные роуты
- `api/login/route.ts` — логин: проксирует на бэкенд и ставит httpOnly-куки `netrunner_token`.
- `api/logout/route.ts` — логаут: сбрасывает куки.
